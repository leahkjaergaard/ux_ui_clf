---
const image = Astro.props.image;
const header = Astro.props.header;
const text = Astro.props.text;
const alt = Astro.props.alt;

const { data } = Astro.props;
console.log(data);
---

<article>
  <img src={image} alt={`Kunstværk i ${alt}`} />
  <h2>{header}</h2>
  <p>{text}</p>
</article>

<style>
  article {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
  }
  img {
    width: 100%;
    height: 90%;
    grid-column: 1/4;
    grid-row: 1/4;
  }
  p {
    grid-column: 1/2;
    grid-row: 2/3;
  }
  h2 {
    grid-column: 1/4;
    grid-row: 2/5;
    padding: 5.2rem 0 0 0.5rem;
  }
  h2 {
    color: white;
    text-decoration: underline;
    font-family: "Jaldi", sans-serif;
    font-weight: 400;
    font-style: normal;
    font-size: 1.2rem;
  }
  p {
    color: white;
    font-family: "Jaldi", sans-serif;
    font-weight: 400;
    font-style: normal;
  }
</style>
